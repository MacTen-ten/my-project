services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "8080:5000"  # Map Host 8080 to Container 5000
  db:
    image: redis:alpine
services:
  app:
    build: .
    volumes:
      - .:/app
  db:
    image: redis:alpine
